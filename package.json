{
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "node scripts/dev-preview.mjs",
    "build:dev": "node scripts/check-copy.mjs && ./apps/web/node_modules/.bin/next build apps/web && node scripts/prepare-dist.mjs",
    "build": "node scripts/build-assistant-index.mjs && node scripts/check-copy.mjs && ./apps/web/node_modules/.bin/next build apps/web && node scripts/prepare-dist.mjs",
    "start": "./apps/web/node_modules/.bin/next start apps/web --port 8080",
    "typecheck": "cd apps/web && ./node_modules/.bin/tsc --noEmit -p tsconfig.json"
  },
  "resolutions": {
    "react": "19.1.0",
    "react-dom": "19.1.0"
  },
  "devDependencies": {
    "eslint": "^9.27.0",
    "oxfmt": "^0.35.0",
    "oxlint": "^1.50.0",
    "typescript-eslint": "^8.33.0",
    "vite": "^7.0.0"
  },
  "packageManager": "yarn@4.12.0"
}
